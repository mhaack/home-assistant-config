---
# Virtual main value sensor for the garden irrigation system to control the
# watertank pump by Hydrawise zone plans.
#
platform: template
sensors:
  garden_irrigation_main_valve:
    entity_id:
      - sensor.beete_next_cycle
      - sensor.hecke_next_cycle
      - sensor.rasen_next_cycle
      - sensor.vorgarten_next_cycle
    friendly_name: Hauptventil Bewässerung
    value_template: >-
      {{ is_state("sensor.beete_next_cycle", "Now") or
        is_state("sensor.hecke_next_cycle", "Now") or
        is_state("sensor.rasen_next_cycle", "Now") or
        is_state("sensor.rasen_next_cycle", "Now") }}
