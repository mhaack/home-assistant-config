---
# History sensor collection the pool pump operation ours.
#
# https://www.home-assistant.io/components/history_stats/
#
platform: history_stats
name: daily_pool_filterpumpe_hours
entity_id: witch.pool_filterpumpe
state: 'on'
type: time
start: '{{ now().replace(hour=0).replace(minute=0).replace(second=0) }}'
end: '{{ now() }}'
