---
# The JaMa Villa network is driven by a bunch of UniFi devices.
# The UniFi controller currently runs on a seperat RPi.
#
# https://www.home-assistant.io/components/unifi/
#

unifi:
  controllers:
    - host: !secret unifi_controler
      site: default
      ssid_filter:
        - !secret wifi1